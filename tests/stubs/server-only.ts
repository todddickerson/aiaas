// Vitest stub for Next.js's `server-only` marker. The real module throws on
// import; in unit tests we just want a no-op so server modules can be tested
// against their JSON fallback paths.
export {};
